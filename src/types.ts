export type FirstNewsCardProps = {
    title: string;
    description: string;
    image: string;
    url: string;
};

export type NewsData = {
    id: number;
    titulo: string;
    introducao: string;
    data_publicacao: string;
    imagens: string;
    link: string;
};